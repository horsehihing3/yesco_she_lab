<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smartehs.mapper.WemImprovementMapper">

    <resultMap id="WemImprovementResultMap" type="com.smartehs.model.WemImprovement">
        <id property="id" column="id"/>
        <result property="processName" column="process_name"/>
        <result property="factorName" column="factor_name"/>
        <result property="measuredValue" column="measured_value"/>
        <result property="exposureStandard" column="exposure_standard"/>
        <result property="exceedRate" column="exceed_rate"/>
        <result property="exceedLevel" column="exceed_level"/>
        <result property="department" column="department"/>
        <result property="measurementDate" column="measurement_date"/>
        <result property="measurementAgency" column="measurement_agency"/>
        <result property="deadline" column="deadline"/>
        <result property="remainingDays" column="remaining_days"/>
        <result property="improvementPlan" column="improvement_plan"/>
        <result property="status" column="status"/>
        <result property="completionDate" column="completion_date"/>
        <result property="remarks" column="remarks"/>
        <result property="createdByUserId" column="created_by_user_id"/>
        <result property="createdByName" column="created_by_name"/>
        <result property="modifiedByUserId" column="modified_by_user_id"/>
        <result property="modifiedByName" column="modified_by_name"/>
        <result property="createdAt" column="created_at"/>
        <result property="modifiedAt" column="modified_at"/>
    </resultMap>

    <select id="findAllWithPaging" resultMap="WemImprovementResultMap">
        SELECT * FROM tb_wem_improvement
        ORDER BY created_at DESC
        OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM tb_wem_improvement
    </select>

    <select id="findByStatus" resultMap="WemImprovementResultMap">
        SELECT * FROM tb_wem_improvement
        WHERE status = #{status}
        ORDER BY created_at DESC
        OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
    </select>

    <select id="countByStatus" resultType="int">
        SELECT COUNT(*) FROM tb_wem_improvement WHERE status = #{status}
    </select>

    <select id="findByExceedLevel" resultMap="WemImprovementResultMap">
        SELECT * FROM tb_wem_improvement
        WHERE exceed_level = #{exceedLevel}
        ORDER BY created_at DESC
        OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
    </select>

    <select id="countByExceedLevel" resultType="int">
        SELECT COUNT(*) FROM tb_wem_improvement WHERE exceed_level = #{exceedLevel}
    </select>

    <select id="findById" resultMap="WemImprovementResultMap">
        SELECT * FROM tb_wem_improvement WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_wem_improvement (process_name, factor_name, measured_value, exposure_standard, exceed_rate, exceed_level, department, measurement_date, measurement_agency, deadline, remaining_days, improvement_plan, status, completion_date, remarks,
            created_by_user_id, created_by_name, modified_by_user_id, modified_by_name,
            created_at, modified_at)
        VALUES (#{processName}, #{factorName}, #{measuredValue}, #{exposureStandard}, #{exceedRate}, #{exceedLevel}, #{department}, #{measurementDate}, #{measurementAgency}, #{deadline}, #{remainingDays}, #{improvementPlan}, #{status}, #{completionDate}, #{remarks},
            #{createdByUserId,jdbcType=BIGINT}, #{createdByName},
            #{modifiedByUserId,jdbcType=BIGINT}, #{modifiedByName},
            GETDATE(), GETDATE())
    </insert>

    <update id="update">
        UPDATE tb_wem_improvement
        SET process_name = #{processName},
            factor_name = #{factorName},
            measured_value = #{measuredValue},
            exposure_standard = #{exposureStandard},
            exceed_rate = #{exceedRate},
            exceed_level = #{exceedLevel},
            department = #{department},
            measurement_date = #{measurementDate},
            measurement_agency = #{measurementAgency},
            deadline = #{deadline},
            remaining_days = #{remainingDays},
            improvement_plan = #{improvementPlan},
            status = #{status},
            completion_date = #{completionDate},
            remarks = #{remarks},
            modified_by_user_id = #{modifiedByUserId,jdbcType=BIGINT},
            modified_by_name = #{modifiedByName},
            modified_at = GETDATE()
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM tb_wem_improvement WHERE id = #{id}
    </delete>

</mapper>
