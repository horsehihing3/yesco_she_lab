<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smartehs.mapper.SiteSafetyPlanMapper">

    <resultMap id="R" type="com.smartehs.model.SiteSafetyPlan">
        <id property="id" column="id"/>
        <result property="planId" column="plan_id"/>
        <result property="planType" column="plan_type"/>
        <result property="title" column="title"/>
        <result property="workType" column="work_type"/>
        <result property="riskLevel" column="risk_level"/>
        <result property="workLocation" column="work_location"/>
        <result property="workersCount" column="workers_count"/>
        <result property="workStartDate" column="work_start_date"/>
        <result property="workEndDate" column="work_end_date"/>
        <result property="workDescription" column="work_description"/>
        <result property="safetyMeasures" column="safety_measures"/>
        <result property="requiredPpe" column="required_ppe"/>
        <result property="hazardFactors" column="hazard_factors"/>
        <result property="emergencyContact" column="emergency_contact"/>
        <result property="notes" column="notes"/>
        <result property="checklistTemplateId" column="checklist_template_id"/>
        <result property="approverName" column="approver_name"/>
        <result property="planApprover" column="plan_approver" typeHandler="com.smartehs.config.typehandler.PersonRefTypeHandler"/>
        <result property="planApprovedAt" column="plan_approved_at"/>
        <result property="planApprovedBy" column="plan_approved_by"/>
        <result property="completionApprover" column="completion_approver" typeHandler="com.smartehs.config.typehandler.PersonRefTypeHandler"/>
        <result property="createdBy"          column="created_by"          typeHandler="com.smartehs.config.typehandler.PersonRefTypeHandler"/>
        <result property="repeatType" column="repeat_type"/>
        <result property="repeatInterval" column="repeat_interval"/>
        <result property="repeatDays" column="repeat_days"/>
        <result property="status" column="status"/>
        <result property="approvedBy" column="approved_by"/>
        <result property="approvedAt" column="approved_at"/>
        <result property="rejectReason" column="reject_reason"/>
        <result property="totalChecklist" column="total_checklist"/>
        <result property="completedChecklist" column="completed_checklist"/>
        <result property="findingCount" column="finding_count"/>
        <result property="modifiedBy"         column="modified_by"/>
        <result property="modifiedByName"     column="modified_by_name"/>
        <result property="modifiedByTeam"     column="modified_by_team"/>
        <result property="modifiedByPosition" column="modified_by_position"/>
        <result property="modifiedByUserId"   column="modified_by_user_id"/>
        <result property="deleted" column="deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="modifiedAt" column="modified_at"/>
        <result property="editingUserId" column="editing_user_id"/>
        <result property="editingUserName" column="editing_user_name"/>
        <result property="editingStartedAt" column="editing_started_at"/>
    </resultMap>

    <select id="findByDeletedFalse" resultMap="R">
        SELECT t.*
        FROM tb_site_safety_plan t
        WHERE t.deleted = 0
        <if test="planType != null">AND t.plan_type = #{planType}</if>
        ORDER BY t.created_at DESC
        OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
    </select>
    <select id="countByDeletedFalse" resultType="int">
        SELECT COUNT(*) FROM tb_site_safety_plan
        WHERE deleted = 0
        <if test="planType != null">AND plan_type = #{planType}</if>
    </select>
    <select id="findById" resultMap="R">
        SELECT t.*
        FROM tb_site_safety_plan t
        WHERE t.id = #{id} AND t.deleted = 0
    </select>
    <select id="findByStatus" resultMap="R">
        SELECT t.*
        FROM tb_site_safety_plan t
        WHERE t.status = #{status} AND t.deleted = 0
        <if test="planType != null">AND t.plan_type = #{planType}</if>
        ORDER BY t.created_at DESC
        OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
    </select>
    <select id="countByStatus" resultType="int">
        SELECT COUNT(*) FROM tb_site_safety_plan
        WHERE status = #{status} AND deleted = 0
        <if test="planType != null">AND plan_type = #{planType}</if>
    </select>
    <select id="countByPlanIdStartingWith" resultType="int">SELECT COUNT(*) FROM tb_site_safety_plan WHERE plan_id LIKE #{prefix} + '%'</select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_site_safety_plan (
            plan_id, plan_type, title, work_type, risk_level, work_location,
            workers_count, work_start_date, work_end_date, work_description, safety_measures,
            required_ppe, hazard_factors, emergency_contact, notes,
            checklist_template_id, approver_name,
            plan_approver, completion_approver, created_by,
            repeat_type, repeat_interval, repeat_days,
            status, total_checklist, completed_checklist, finding_count,
            modified_by, deleted, created_at, modified_at
        ) VALUES (
            #{planId}, COALESCE(#{planType}, 'INTERNAL'), #{title}, #{workType}, #{riskLevel}, #{workLocation},
            #{workersCount}, #{workStartDate,jdbcType=DATE}, #{workEndDate,jdbcType=DATE},
            #{workDescription}, #{safetyMeasures}, #{requiredPpe}, #{hazardFactors},
            #{emergencyContact}, #{notes}, #{checklistTemplateId}, #{approverName},
            #{planApprover,typeHandler=com.smartehs.config.typehandler.PersonRefTypeHandler},
            #{completionApprover,typeHandler=com.smartehs.config.typehandler.PersonRefTypeHandler},
            #{createdBy,typeHandler=com.smartehs.config.typehandler.PersonRefTypeHandler},
            #{repeatType}, #{repeatInterval}, #{repeatDays},
            #{status}, 0, 0, 0, #{modifiedBy}, 0, GETDATE(), GETDATE()
        )
    </insert>

    <update id="update">
        UPDATE tb_site_safety_plan SET
            title = #{title}, work_type = #{workType}, risk_level = #{riskLevel},
            work_location = #{workLocation}, workers_count = #{workersCount},
            work_start_date = #{workStartDate,jdbcType=DATE}, work_end_date = #{workEndDate,jdbcType=DATE},
            work_description = #{workDescription}, safety_measures = #{safetyMeasures},
            required_ppe = #{requiredPpe}, hazard_factors = #{hazardFactors},
            emergency_contact = #{emergencyContact}, notes = #{notes},
            checklist_template_id = #{checklistTemplateId}, approver_name = #{approverName},
            plan_approver       = #{planApprover,typeHandler=com.smartehs.config.typehandler.PersonRefTypeHandler},
            completion_approver = #{completionApprover,typeHandler=com.smartehs.config.typehandler.PersonRefTypeHandler},
            repeat_type = #{repeatType}, repeat_interval = #{repeatInterval}, repeat_days = #{repeatDays},
            status = COALESCE(#{status}, status, 'DRAFT'),
            modified_by = #{modifiedBy}, modified_at = GETDATE(),
            modified_by_name = #{modifiedByName}, modified_by_team = #{modifiedByTeam},
            modified_by_position = #{modifiedByPosition}, modified_by_user_id = #{modifiedByUserId,jdbcType=BIGINT}
        WHERE id = #{id}
    </update>

    <update id="transition">
        UPDATE tb_site_safety_plan
        SET status = #{status},
            plan_approved_at = CASE WHEN #{stage} = 'PLAN' AND #{approved} = 1 THEN GETDATE() ELSE plan_approved_at END,
            plan_approved_by = CASE WHEN #{stage} = 'PLAN' AND #{approved} = 1 THEN #{approvedBy} ELSE plan_approved_by END,
            approved_by  = CASE WHEN #{stage} = 'PLAN' AND #{approved} = 1 THEN #{approvedBy} ELSE approved_by END,
            approved_at  = CASE WHEN #{stage} = 'PLAN' AND #{approved} = 1 THEN GETDATE()    ELSE approved_at END,
            <!-- 반려 시 사유 저장, 재상신·승인 등 다음 전이에서 제거 -->
            reject_reason = #{rejectReason},
            modified_at = GETDATE()
        WHERE id = #{id}
    </update>

    <update id="softDelete">UPDATE tb_site_safety_plan SET deleted = 1, modified_at = GETDATE() WHERE id = #{id}</update>

    <update id="acquireEditLock">
        UPDATE tb_site_safety_plan
        SET editing_user_id = #{userId}, editing_user_name = #{userName}, editing_started_at = GETDATE()
        WHERE id = #{id}
    </update>
    <update id="releaseEditLock">
        UPDATE tb_site_safety_plan
        SET editing_user_id = NULL, editing_user_name = NULL, editing_started_at = NULL
        WHERE id = #{id} AND (editing_user_id = #{userId} OR editing_user_id IS NULL)
    </update>
</mapper>
