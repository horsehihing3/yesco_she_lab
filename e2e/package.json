{
  "name": "smart-ehs-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Smart EHS 기능 시나리오 자동화 (Playwright) - 결재 라이프사이클 E2E",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:approval": "playwright test annual-plan-approval",
    "report": "playwright show-report",
    "install:browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
